spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/monitoreo_estres_db
    username: postgres
    password: tu_password_local
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
